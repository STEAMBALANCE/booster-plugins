{
  "name": "@booster-plugins/monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "gen-strings": "bun run scripts/gen-strings.ts",
    "build": "bun run --filter '*' build",
    "test": "bun test --cwd packages/booster-checkout && bun test --cwd packages/booster-addfunds"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "@steambalance/booster-framework": "file:../booster-framework",
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}
